# 后端 Dockerfile
FROM node:18-slim

# 设置工作目录
WORKDIR /app

# 复制 package 文件
COPY server/package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源代码
COPY server/ ./

# 构建 TypeScript
RUN npm run build

# 创建数据目录
RUN mkdir -p data uploads

# 暴露端口
EXPOSE 3001

# 启动命令
CMD ["npm", "start"]
