# 前端 Dockerfile (开发模式)
FROM node:18-slim

# 设置工作目录
WORKDIR /app

# 复制 package 文件
COPY client/package*.json ./

# 安装依赖
RUN npm ci

# 复制源代码
COPY client/ ./

# 暴露端口
EXPOSE 5177

# 启动命令 (Vite 开发服务器)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
